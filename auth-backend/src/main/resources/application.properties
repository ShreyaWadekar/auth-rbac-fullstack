# Server
server.port=8080

# H2 In-memory DB (easy for development)
spring.datasource.url=jdbc:h2:mem:authdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# JWT Secret (use a long random string, min 256 bits)
jwt.secret=dGhpc2lzYXZlcnlsb25nc2VjcmV0a2V5Zm9yand0dG9rZW5nZW5lcmF0aW9u
jwt.expiration=86400000
```

---

### Step 5: Create the Folder Structure

Inside `src/main/java/com/example/authbackend/`, create these packages:
```
authbackend/
├── config/
├── controller/
├── dto/
│   ├── request/
│   └── response/
├── entity/
├── mapper/
├── repository/
├── security/
│   └── jwt/
└── service/